{
  "id": "react-hooks",
  "category": "react",
  "title": "Hooks応用 - useContext, useRef, useMemo, useCallback",
  "description": "useState/useEffect以外の主要なReact Hooksと、カスタムHooksの作成方法を学びましょう",
  "content": "<div class=\"content-section\"><h2 class=\"section-title\">React Hooksの全体像</h2><p class=\"content-text\">これまでuseStateとuseEffectを学びましたが、Reactには他にも便利なHooksがあります。この章では、実践でよく使う<strong>useContext</strong>、<strong>useRef</strong>、<strong>useMemo</strong>、<strong>useCallback</strong>と、<strong>カスタムHooks</strong>の作成方法を学びます。</p><div class=\"info-box\"><span class=\"info-box-icon\">💡</span><div class=\"info-box-content\"><div class=\"info-box-title\">Hooksのルール（復習）</div><div class=\"info-box-text\">1. Hooksはコンポーネントのトップレベルでのみ呼び出す<br/>2. Hooksは関数コンポーネントまたはカスタムHooks内でのみ使用する<br/>3. 条件分岐やループの中でHooksを呼び出さない</div></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">useContext - グローバルな状態共有</h2><p class=\"content-text\"><strong>useContext</strong>は、コンポーネントツリー全体でデータを共有するためのHookです。Props drilling（バケツリレー）を避けて、深い階層のコンポーネントにデータを渡せます。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>import { createContext, useContext, useState } from 'react';\n\n// 1. Contextを作成\nconst ThemeContext = createContext('light');\n\n// 2. Providerコンポーネントで値を提供\nfunction App() {\n  const [theme, setTheme] = useState('light');\n\n  return (\n    &lt;ThemeContext.Provider value={theme}&gt;\n      &lt;Header /&gt;\n      &lt;Main /&gt;\n      &lt;button onClick={() =&gt; setTheme(theme === 'light' ? 'dark' : 'light')}&gt;\n        テーマ切替\n      &lt;/button&gt;\n    &lt;/ThemeContext.Provider&gt;\n  );\n}\n\n// 3. useContextで値を取得（どの階層からでもOK）\nfunction Header() {\n  const theme = useContext(ThemeContext);\n  return &lt;header className={theme}&gt;ヘッダー&lt;/header&gt;;\n}\n\nfunction Main() {\n  const theme = useContext(ThemeContext);\n  return &lt;main className={theme}&gt;メインコンテンツ&lt;/main&gt;;\n}</code></pre></div></div><div class=\"info-box\"><span class=\"info-box-icon\">💡</span><div class=\"info-box-content\"><div class=\"info-box-title\">Contextの使いどころ</div><div class=\"info-box-text\">テーマ、認証情報、言語設定など、アプリ全体で共有したいデータに適しています。ただし、頻繁に更新されるデータには向きません（再レンダリングが多発するため）。</div></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">Contextの実践パターン</h2><p class=\"content-text\">値と更新関数を一緒に提供するパターンがよく使われます：</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>// AuthContext.jsx\nimport { createContext, useContext, useState } from 'react';\n\nconst AuthContext = createContext(null);\n\n// カスタムProviderコンポーネント\nexport function AuthProvider({ children }) {\n  const [user, setUser] = useState(null);\n\n  const login = (userData) =&gt; setUser(userData);\n  const logout = () =&gt; setUser(null);\n\n  // 値と関数をまとめて提供\n  const value = { user, login, logout };\n\n  return (\n    &lt;AuthContext.Provider value={value}&gt;\n      {children}\n    &lt;/AuthContext.Provider&gt;\n  );\n}\n\n// カスタムHookで使いやすく\nexport function useAuth() {\n  const context = useContext(AuthContext);\n  if (!context) {\n    throw new Error('useAuth must be used within AuthProvider');\n  }\n  return context;\n}\n\n// 使用例\nfunction Profile() {\n  const { user, logout } = useAuth();\n  \n  if (!user) return &lt;p&gt;ログインしてください&lt;/p&gt;;\n  \n  return (\n    &lt;div&gt;\n      &lt;p&gt;ようこそ、{user.name}さん&lt;/p&gt;\n      &lt;button onClick={logout}&gt;ログアウト&lt;/button&gt;\n    &lt;/div&gt;\n  );\n}</code></pre></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">useRef - DOM参照と値の保持</h2><p class=\"content-text\"><strong>useRef</strong>は2つの主な用途があります：</p><ul><li><strong>DOM要素への直接アクセス</strong></li><li><strong>レンダリング間で値を保持</strong>（再レンダリングを引き起こさない）</li></ul><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>import { useRef, useEffect } from 'react';\n\n// 用途1: DOM要素への参照\nfunction TextInput() {\n  const inputRef = useRef(null);\n\n  const focusInput = () =&gt; {\n    inputRef.current.focus();  // DOM APIを直接呼び出し\n  };\n\n  return (\n    &lt;div&gt;\n      &lt;input ref={inputRef} type=\"text\" /&gt;\n      &lt;button onClick={focusInput}&gt;フォーカス&lt;/button&gt;\n    &lt;/div&gt;\n  );\n}\n\n// 用途2: 値の保持（再レンダリングなし）\nfunction Timer() {\n  const [count, setCount] = useState(0);\n  const intervalRef = useRef(null);\n\n  const startTimer = () =&gt; {\n    intervalRef.current = setInterval(() =&gt; {\n      setCount(prev =&gt; prev + 1);\n    }, 1000);\n  };\n\n  const stopTimer = () =&gt; {\n    clearInterval(intervalRef.current);\n  };\n\n  return (\n    &lt;div&gt;\n      &lt;p&gt;カウント: {count}&lt;/p&gt;\n      &lt;button onClick={startTimer}&gt;開始&lt;/button&gt;\n      &lt;button onClick={stopTimer}&gt;停止&lt;/button&gt;\n    &lt;/div&gt;\n  );\n}</code></pre></div></div><div class=\"info-box\"><span class=\"info-box-icon\">⚠️</span><div class=\"info-box-content\"><div class=\"info-box-title\">useRefとuseStateの違い</div><div class=\"info-box-text\">useRef: .currentを変更しても再レンダリングされない<br/>useState: 値を変更すると再レンダリングされる<br/><br/>UIに反映する必要がない値はuseRefを使いましょう。</div></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">useRefの実践例</h2><p class=\"content-text\">前回の値を保持する例：</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>function Counter() {\n  const [count, setCount] = useState(0);\n  const prevCountRef = useRef(0);\n\n  useEffect(() =&gt; {\n    prevCountRef.current = count;  // レンダリング後に更新\n  });\n\n  return (\n    &lt;div&gt;\n      &lt;p&gt;現在: {count}, 前回: {prevCountRef.current}&lt;/p&gt;\n      &lt;button onClick={() =&gt; setCount(count + 1)}&gt;増やす&lt;/button&gt;\n    &lt;/div&gt;\n  );\n}\n\n// スクロール位置の制御\nfunction ChatMessages({ messages }) {\n  const messagesEndRef = useRef(null);\n\n  const scrollToBottom = () =&gt; {\n    messagesEndRef.current?.scrollIntoView({ behavior: 'smooth' });\n  };\n\n  useEffect(() =&gt; {\n    scrollToBottom();\n  }, [messages]);\n\n  return (\n    &lt;div className=\"messages\"&gt;\n      {messages.map(msg =&gt; &lt;p key={msg.id}&gt;{msg.text}&lt;/p&gt;)}\n      &lt;div ref={messagesEndRef} /&gt;\n    &lt;/div&gt;\n  );\n}</code></pre></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">useMemo - 計算結果のメモ化</h2><p class=\"content-text\"><strong>useMemo</strong>は、重い計算の結果をキャッシュし、依存配列が変わらない限り再計算を避けます。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>import { useMemo, useState } from 'react';\n\nfunction ExpensiveList({ items, filter }) {\n  // filterが変わらない限り、filteredItemsは再計算されない\n  const filteredItems = useMemo(() =&gt; {\n    console.log('フィルタリング実行');\n    return items.filter(item =&gt; \n      item.name.toLowerCase().includes(filter.toLowerCase())\n    );\n  }, [items, filter]);  // itemsまたはfilterが変わったら再計算\n\n  return (\n    &lt;ul&gt;\n      {filteredItems.map(item =&gt; (\n        &lt;li key={item.id}&gt;{item.name}&lt;/li&gt;\n      ))}\n    &lt;/ul&gt;\n  );\n}\n\n// 使用例\nfunction App() {\n  const [filter, setFilter] = useState('');\n  const [count, setCount] = useState(0);\n  const items = [/* 大量のデータ */];\n\n  return (\n    &lt;div&gt;\n      &lt;input \n        value={filter} \n        onChange={e =&gt; setFilter(e.target.value)} \n      /&gt;\n      {/* countが変わってもフィルタリングは再実行されない */}\n      &lt;button onClick={() =&gt; setCount(count + 1)}&gt;\n        カウント: {count}\n      &lt;/button&gt;\n      &lt;ExpensiveList items={items} filter={filter} /&gt;\n    &lt;/div&gt;\n  );\n}</code></pre></div></div><div class=\"info-box\"><span class=\"info-box-icon\">⚠️</span><div class=\"info-box-content\"><div class=\"info-box-title\">useMemoの注意点</div><div class=\"info-box-text\">すべての計算にuseMemoを使う必要はありません。メモ化自体にもコストがあるため、本当に重い計算や、子コンポーネントへの参照の安定化が必要な場合に使いましょう。</div></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">useCallback - 関数のメモ化</h2><p class=\"content-text\"><strong>useCallback</strong>は、関数自体をメモ化します。子コンポーネントに関数を渡す際、不要な再レンダリングを防ぐのに役立ちます。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>import { useCallback, useState, memo } from 'react';\n\n// memo化された子コンポーネント\nconst Button = memo(function Button({ onClick, children }) {\n  console.log('Button rendered:', children);\n  return &lt;button onClick={onClick}&gt;{children}&lt;/button&gt;;\n});\n\nfunction Parent() {\n  const [count, setCount] = useState(0);\n  const [text, setText] = useState('');\n\n  // ❌ 毎回新しい関数が作られる → Buttonが毎回再レンダリング\n  const handleClickBad = () =&gt; {\n    setCount(count + 1);\n  };\n\n  // ✅ useCallbackで関数をメモ化 → Buttonの再レンダリングを防ぐ\n  const handleClickGood = useCallback(() =&gt; {\n    setCount(prev =&gt; prev + 1);\n  }, []);  // 依存配列が空 = 関数は変わらない\n\n  return (\n    &lt;div&gt;\n      &lt;input value={text} onChange={e =&gt; setText(e.target.value)} /&gt;\n      &lt;p&gt;カウント: {count}&lt;/p&gt;\n      {/* textが変わってもButtonは再レンダリングされない */}\n      &lt;Button onClick={handleClickGood}&gt;増やす&lt;/Button&gt;\n    &lt;/div&gt;\n  );\n}</code></pre></div></div><p class=\"content-text\">useMemoとuseCallbackの関係：</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>// この2つは同じ意味\nconst memoizedFn = useCallback(fn, deps);\nconst memoizedFn = useMemo(() =&gt; fn, deps);</code></pre></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">カスタムHooksの作成</h2><p class=\"content-text\"><strong>カスタムHooks</strong>は、ロジックを再利用可能な関数として抽出したものです。「use」で始まる名前をつけます。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>// useLocalStorage - localStorageと同期するstate\nfunction useLocalStorage(key, initialValue) {\n  const [storedValue, setStoredValue] = useState(() =&gt; {\n    try {\n      const item = localStorage.getItem(key);\n      return item ? JSON.parse(item) : initialValue;\n    } catch (error) {\n      return initialValue;\n    }\n  });\n\n  const setValue = (value) =&gt; {\n    try {\n      const valueToStore = value instanceof Function \n        ? value(storedValue) \n        : value;\n      setStoredValue(valueToStore);\n      localStorage.setItem(key, JSON.stringify(valueToStore));\n    } catch (error) {\n      console.error(error);\n    }\n  };\n\n  return [storedValue, setValue];\n}\n\n// 使用例\nfunction App() {\n  const [name, setName] = useLocalStorage('name', '');\n  \n  return (\n    &lt;input \n      value={name} \n      onChange={e =&gt; setName(e.target.value)} \n    /&gt;\n  );\n}</code></pre></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">実践的なカスタムHooks</h2><p class=\"content-text\">よく使われるカスタムHooksの例：</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>// useFetch - データ取得\nfunction useFetch(url) {\n  const [data, setData] = useState(null);\n  const [loading, setLoading] = useState(true);\n  const [error, setError] = useState(null);\n\n  useEffect(() =&gt; {\n    const fetchData = async () =&gt; {\n      try {\n        setLoading(true);\n        const response = await fetch(url);\n        if (!response.ok) throw new Error('Fetch failed');\n        const json = await response.json();\n        setData(json);\n      } catch (err) {\n        setError(err.message);\n      } finally {\n        setLoading(false);\n      }\n    };\n    fetchData();\n  }, [url]);\n\n  return { data, loading, error };\n}\n\n// 使用例\nfunction UserList() {\n  const { data, loading, error } = useFetch('/api/users');\n\n  if (loading) return &lt;p&gt;読み込み中...&lt;/p&gt;;\n  if (error) return &lt;p&gt;エラー: {error}&lt;/p&gt;;\n\n  return (\n    &lt;ul&gt;\n      {data.map(user =&gt; &lt;li key={user.id}&gt;{user.name}&lt;/li&gt;)}\n    &lt;/ul&gt;\n  );\n}</code></pre></div></div><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>// useWindowSize - ウィンドウサイズの監視\nfunction useWindowSize() {\n  const [size, setSize] = useState({\n    width: window.innerWidth,\n    height: window.innerHeight\n  });\n\n  useEffect(() =&gt; {\n    const handleResize = () =&gt; {\n      setSize({\n        width: window.innerWidth,\n        height: window.innerHeight\n      });\n    };\n\n    window.addEventListener('resize', handleResize);\n    return () =&gt; window.removeEventListener('resize', handleResize);\n  }, []);\n\n  return size;\n}\n\n// 使用例\nfunction ResponsiveComponent() {\n  const { width } = useWindowSize();\n  \n  return (\n    &lt;div&gt;\n      {width &lt; 768 ? &lt;MobileView /&gt; : &lt;DesktopView /&gt;}\n    &lt;/div&gt;\n  );\n}</code></pre></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">useToggle - シンプルなカスタムHook</h2><p class=\"content-text\">真偽値の切り替えを簡単にするカスタムHook：</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>function useToggle(initialValue = false) {\n  const [value, setValue] = useState(initialValue);\n  \n  const toggle = useCallback(() =&gt; {\n    setValue(prev =&gt; !prev);\n  }, []);\n  \n  const setTrue = useCallback(() =&gt; setValue(true), []);\n  const setFalse = useCallback(() =&gt; setValue(false), []);\n\n  return { value, toggle, setTrue, setFalse };\n}\n\n// 使用例\nfunction Modal() {\n  const { value: isOpen, toggle, setFalse: close } = useToggle();\n\n  return (\n    &lt;div&gt;\n      &lt;button onClick={toggle}&gt;モーダルを開く&lt;/button&gt;\n      {isOpen && (\n        &lt;div className=\"modal\"&gt;\n          &lt;p&gt;モーダルの内容&lt;/p&gt;\n          &lt;button onClick={close}&gt;閉じる&lt;/button&gt;\n        &lt;/div&gt;\n      )}\n    &lt;/div&gt;\n  );\n}</code></pre></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">Hooksの使い分けガイド</h2><p class=\"content-text\">どのHookを使うべきか迷ったときの指針：</p><ul><li><strong>useState</strong> - UIに反映される状態を管理</li><li><strong>useEffect</strong> - 副作用（API呼び出し、イベントリスナー等）</li><li><strong>useContext</strong> - 複数コンポーネントでデータを共有</li><li><strong>useRef</strong> - DOM参照、または再レンダリング不要な値の保持</li><li><strong>useMemo</strong> - 重い計算結果のキャッシュ</li><li><strong>useCallback</strong> - 子コンポーネントに渡す関数の安定化</li></ul><div class=\"info-box\"><span class=\"info-box-icon\">💡</span><div class=\"info-box-content\"><div class=\"info-box-title\">最適化の注意点</div><div class=\"info-box-text\">useMemoやuseCallbackは「早すぎる最適化」に注意。まずは普通に書いて、パフォーマンス問題が発生してから最適化を検討しましょう。</div></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">まとめ</h2><p class=\"content-text\">この章で学んだこと：</p><ul><li><strong>useContext</strong> - Props drillingを避けてデータを共有</li><li><strong>useRef</strong> - DOM参照と再レンダリングなしの値保持</li><li><strong>useMemo</strong> - 計算結果のメモ化</li><li><strong>useCallback</strong> - 関数のメモ化</li><li><strong>カスタムHooks</strong> - ロジックの再利用</li></ul><p class=\"content-text\">次のレッスンでは、Reactの<strong>実践的なパターンとベストプラクティス</strong>について学びます。</p></div>",
  "exercises": [
    {
      "question": "useContextの主な用途として正しいものはどれですか？",
      "options": [
        "DOM要素への直接アクセス",
        "計算結果のキャッシュ",
        "Props drilling（バケツリレー）を避けてデータを共有する",
        "副作用の実行"
      ],
      "answer": 2,
      "explanation": "useContextは、コンポーネントツリーの深い階層にデータを渡す際に、中間のコンポーネントを経由せずに直接データを共有できます。これにより、Props drilling（バケツリレー）を避けることができます。"
    },
    {
      "question": "useRefの特徴として正しいものはどれですか？",
      "options": [
        "値が変更されると再レンダリングが発生する",
        "値が変更されても再レンダリングは発生しない",
        "関数コンポーネントでは使用できない",
        "初期値を設定できない"
      ],
      "answer": 1,
      "explanation": "useRefは.currentプロパティに値を保持しますが、その値が変更されても再レンダリングは発生しません。これがuseStateとの大きな違いです。DOM要素への参照や、レンダリング間で保持したい値に使用します。"
    },
    {
      "question": "useMemoとuseCallbackの違いとして正しいものはどれですか？",
      "options": [
        "useMemoは値をメモ化し、useCallbackは関数をメモ化する",
        "useMemoは関数をメモ化し、useCallbackは値をメモ化する",
        "両方とも同じ機能で、名前が違うだけ",
        "useMemoは同期処理、useCallbackは非同期処理に使う"
      ],
      "answer": 0,
      "explanation": "useMemoは計算結果（値）をメモ化し、useCallbackは関数自体をメモ化します。useMemo(() => computeValue(), deps)は値を返し、useCallback(fn, deps)は関数を返します。"
    },
    {
      "question": "カスタムHooksの命名規則として正しいものはどれですか？",
      "options": [
        "custom で始める（例：customFetch）",
        "use で始める（例：useFetch）",
        "hook で始める（例：hookFetch）",
        "特に規則はない"
      ],
      "answer": 1,
      "explanation": "カスタムHooksは必ず「use」で始める命名規則があります。これにより、ReactがHooksのルールを適用でき、ESLintなどのツールも正しく動作します。例：useFetch、useLocalStorage、useWindowSizeなど。"
    }
  ]
}
{
  "id": "react-state",
  "category": "react",
  "title": "useStateによる状態管理",
  "description": "useStateフックの基本的な使い方、関数型更新、オブジェクトや配列の状態管理、状態のリフトアップについて学びましょう",
  "content": "<div class=\"content-section\"><h2 class=\"section-title\">状態（State）とは？</h2><p class=\"content-text\">Reactの<strong>状態（State）</strong>は、コンポーネントが「記憶」できるデータです。Propsは親から渡される読み取り専用のデータでしたが、Stateはコンポーネント自身が管理し、変更できるデータです。</p><p class=\"content-text\">状態が変更されると、Reactは自動的にコンポーネントを<strong>再レンダリング</strong>し、UIを更新します。</p><div class=\"info-box\"><span class=\"info-box-icon\">💡</span><div class=\"info-box-content\"><div class=\"info-box-title\">PropsとStateの違い</div><div class=\"info-box-text\">Props: 親から渡される、読み取り専用<br/>State: コンポーネント自身が管理、変更可能</div></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">useStateの基本</h2><p class=\"content-text\"><strong>useState</strong>は、関数コンポーネントで状態を管理するためのReact Hookです。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>import { useState } from 'react';\n\nfunction Counter() {\n  // useStateは [現在の値, 更新関数] の配列を返す\n  const [count, setCount] = useState(0);\n\n  return (\n    &lt;div&gt;\n      &lt;p&gt;カウント: {count}&lt;/p&gt;\n      &lt;button onClick={() =&gt; setCount(count + 1)}&gt;\n        増やす\n      &lt;/button&gt;\n    &lt;/div&gt;\n  );\n}</code></pre></div></div><p class=\"content-text\">useStateの構文を分解すると：</p><ul><li><strong>useState(0)</strong> - 初期値を0に設定</li><li><strong>count</strong> - 現在の状態値</li><li><strong>setCount</strong> - 状態を更新する関数</li></ul><div class=\"info-box\"><span class=\"info-box-icon\">⚠️</span><div class=\"info-box-content\"><div class=\"info-box-title\">重要なルール</div><div class=\"info-box-text\">状態を直接変更してはいけません。必ず更新関数（setCount等）を使います。<br/>❌ count = count + 1<br/>✅ setCount(count + 1)</div></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">様々な型の状態</h2><p class=\"content-text\">useStateは様々な型のデータを管理できます：</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>// 数値\nconst [count, setCount] = useState(0);\n\n// 文字列\nconst [name, setName] = useState('');\n\n// 真偽値\nconst [isOpen, setIsOpen] = useState(false);\n\n// 配列\nconst [items, setItems] = useState([]);\n\n// オブジェクト\nconst [user, setUser] = useState({ name: '', age: 0 });\n\n// null許容\nconst [data, setData] = useState(null);</code></pre></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">関数型更新</h2><p class=\"content-text\">状態の更新が<strong>前の状態に依存する</strong>場合は、<strong>関数型更新</strong>を使うべきです。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>function Counter() {\n  const [count, setCount] = useState(0);\n\n  // ❌ 問題のあるコード（連続クリックで期待通り動かない場合がある）\n  const handleClickBad = () =&gt; {\n    setCount(count + 1);\n    setCount(count + 1);  // countは古い値のまま\n  };\n\n  // ✅ 関数型更新（前の状態を確実に参照）\n  const handleClickGood = () =&gt; {\n    setCount(prev =&gt; prev + 1);\n    setCount(prev =&gt; prev + 1);  // 正しく2増える\n  };\n\n  return (\n    &lt;div&gt;\n      &lt;p&gt;カウント: {count}&lt;/p&gt;\n      &lt;button onClick={handleClickGood}&gt;+2&lt;/button&gt;\n    &lt;/div&gt;\n  );\n}</code></pre></div></div><div class=\"info-box\"><span class=\"info-box-icon\">💡</span><div class=\"info-box-content\"><div class=\"info-box-title\">なぜ関数型更新が必要？</div><div class=\"info-box-text\">Reactは状態更新をバッチ処理するため、同じレンダリング内で複数回setCountを呼ぶと、古い値を参照してしまいます。関数型更新なら、常に最新の状態を参照できます。</div></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">オブジェクトの状態管理</h2><p class=\"content-text\">オブジェクトの状態を更新する際は、<strong>新しいオブジェクトを作成</strong>する必要があります。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>function UserForm() {\n  const [user, setUser] = useState({\n    name: '',\n    email: '',\n    age: 0\n  });\n\n  // ❌ 間違い：直接変更\n  const handleChangeBad = (e) =&gt; {\n    user.name = e.target.value;  // 再レンダリングされない！\n  };\n\n  // ✅ 正しい：スプレッド構文で新しいオブジェクトを作成\n  const handleNameChange = (e) =&gt; {\n    setUser({\n      ...user,           // 既存のプロパティをコピー\n      name: e.target.value  // nameだけ上書き\n    });\n  };\n\n  // ✅ 関数型更新と組み合わせ\n  const handleEmailChange = (e) =&gt; {\n    setUser(prev =&gt; ({\n      ...prev,\n      email: e.target.value\n    }));\n  };\n\n  return (\n    &lt;form&gt;\n      &lt;input\n        value={user.name}\n        onChange={handleNameChange}\n        placeholder=\"名前\"\n      /&gt;\n      &lt;input\n        value={user.email}\n        onChange={handleEmailChange}\n        placeholder=\"メール\"\n      /&gt;\n    &lt;/form&gt;\n  );\n}</code></pre></div></div><div class=\"info-box\"><span class=\"info-box-icon\">⚠️</span><div class=\"info-box-content\"><div class=\"info-box-title\">イミュータブル更新</div><div class=\"info-box-text\">Reactは状態の参照が変わったかどうかで再レンダリングを判断します。オブジェクトを直接変更しても、参照は同じなので再レンダリングされません。</div></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">ネストしたオブジェクトの更新</h2><p class=\"content-text\">ネストしたオブジェクトの更新は少し複雑になります：</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>const [user, setUser] = useState({\n  name: '田中',\n  address: {\n    city: '東京',\n    zip: '100-0001'\n  }\n});\n\n// ネストしたプロパティを更新\nconst updateCity = (newCity) =&gt; {\n  setUser(prev =&gt; ({\n    ...prev,\n    address: {\n      ...prev.address,  // addressの他のプロパティを保持\n      city: newCity\n    }\n  }));\n};</code></pre></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">配列の状態管理</h2><p class=\"content-text\">配列も同様に、<strong>新しい配列を作成</strong>して更新します。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>function TodoList() {\n  const [todos, setTodos] = useState([\n    { id: 1, text: '買い物', done: false },\n    { id: 2, text: '掃除', done: true }\n  ]);\n\n  // 追加\n  const addTodo = (text) =&gt; {\n    setTodos(prev =&gt; [\n      ...prev,\n      { id: Date.now(), text, done: false }\n    ]);\n  };\n\n  // 削除\n  const removeTodo = (id) =&gt; {\n    setTodos(prev =&gt; prev.filter(todo =&gt; todo.id !== id));\n  };\n\n  // 更新（特定のアイテムを変更）\n  const toggleTodo = (id) =&gt; {\n    setTodos(prev =&gt; prev.map(todo =&gt;\n      todo.id === id\n        ? { ...todo, done: !todo.done }\n        : todo\n    ));\n  };\n\n  return (\n    &lt;ul&gt;\n      {todos.map(todo =&gt; (\n        &lt;li key={todo.id}&gt;\n          &lt;span style={{ textDecoration: todo.done ? 'line-through' : 'none' }}&gt;\n            {todo.text}\n          &lt;/span&gt;\n          &lt;button onClick={() =&gt; toggleTodo(todo.id)}&gt;切替&lt;/button&gt;\n          &lt;button onClick={() =&gt; removeTodo(todo.id)}&gt;削除&lt;/button&gt;\n        &lt;/li&gt;\n      ))}\n    &lt;/ul&gt;\n  );\n}</code></pre></div></div><p class=\"content-text\">配列操作のパターン：</p><ul><li><strong>追加</strong>: [...prev, newItem] または [newItem, ...prev]</li><li><strong>削除</strong>: prev.filter(item => item.id !== targetId)</li><li><strong>更新</strong>: prev.map(item => item.id === targetId ? {...item, ...changes} : item)</li><li><strong>並べ替え</strong>: [...prev].sort((a, b) => ...)</li></ul></div><div class=\"content-section\"><h2 class=\"section-title\">複数のuseState</h2><p class=\"content-text\">1つのコンポーネントで複数のuseStateを使えます：</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>function SignupForm() {\n  // 関連性の低いデータは別々のstateに\n  const [name, setName] = useState('');\n  const [email, setEmail] = useState('');\n  const [isSubmitting, setIsSubmitting] = useState(false);\n  const [errors, setErrors] = useState({});\n\n  const handleSubmit = async (e) =&gt; {\n    e.preventDefault();\n    setIsSubmitting(true);\n    setErrors({});\n    \n    try {\n      await submitForm({ name, email });\n    } catch (err) {\n      setErrors({ submit: err.message });\n    } finally {\n      setIsSubmitting(false);\n    }\n  };\n\n  return (\n    &lt;form onSubmit={handleSubmit}&gt;\n      &lt;input value={name} onChange={e =&gt; setName(e.target.value)} /&gt;\n      &lt;input value={email} onChange={e =&gt; setEmail(e.target.value)} /&gt;\n      &lt;button disabled={isSubmitting}&gt;\n        {isSubmitting ? '送信中...' : '送信'}\n      &lt;/button&gt;\n    &lt;/form&gt;\n  );\n}</code></pre></div></div><div class=\"info-box\"><span class=\"info-box-icon\">💡</span><div class=\"info-box-content\"><div class=\"info-box-title\">stateの分割基準</div><div class=\"info-box-text\">一緒に更新されることが多いデータはまとめ、独立して更新されるデータは分けるのがベストプラクティスです。</div></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">状態のリフトアップ</h2><p class=\"content-text\">複数のコンポーネントで同じ状態を共有したい場合、状態を<strong>共通の親コンポーネントに移動</strong>します。これを<strong>状態のリフトアップ</strong>と呼びます。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>// 子コンポーネント：状態を持たない\nfunction TemperatureInput({ scale, temperature, onTemperatureChange }) {\n  const scaleNames = { c: '摂氏', f: '華氏' };\n  \n  return (\n    &lt;fieldset&gt;\n      &lt;legend&gt;{scaleNames[scale]}で入力:&lt;/legend&gt;\n      &lt;input\n        value={temperature}\n        onChange={e =&gt; onTemperatureChange(e.target.value)}\n      /&gt;\n    &lt;/fieldset&gt;\n  );\n}\n\n// 親コンポーネント：状態を管理\nfunction Calculator() {\n  const [temperature, setTemperature] = useState('');\n  const [scale, setScale] = useState('c');\n\n  const handleCelsiusChange = (temp) =&gt; {\n    setScale('c');\n    setTemperature(temp);\n  };\n\n  const handleFahrenheitChange = (temp) =&gt; {\n    setScale('f');\n    setTemperature(temp);\n  };\n\n  // 変換計算\n  const celsius = scale === 'f' ? tryConvert(temperature, toCelsius) : temperature;\n  const fahrenheit = scale === 'c' ? tryConvert(temperature, toFahrenheit) : temperature;\n\n  return (\n    &lt;div&gt;\n      &lt;TemperatureInput\n        scale=\"c\"\n        temperature={celsius}\n        onTemperatureChange={handleCelsiusChange}\n      /&gt;\n      &lt;TemperatureInput\n        scale=\"f\"\n        temperature={fahrenheit}\n        onTemperatureChange={handleFahrenheitChange}\n      /&gt;\n    &lt;/div&gt;\n  );\n}</code></pre></div></div><div class=\"info-box\"><span class=\"info-box-icon\">💡</span><div class=\"info-box-content\"><div class=\"info-box-title\">リフトアップのポイント</div><div class=\"info-box-text\">状態を親に移動し、子には値と更新関数をPropsで渡します。これにより、複数の子コンポーネントが同じ状態を共有できます。</div></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">制御コンポーネント vs 非制御コンポーネント</h2><p class=\"content-text\">フォーム要素の状態管理には2つのアプローチがあります：</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>// 制御コンポーネント：Reactが状態を管理\nfunction ControlledInput() {\n  const [value, setValue] = useState('');\n  \n  return (\n    &lt;input\n      value={value}  // Reactが値を制御\n      onChange={e =&gt; setValue(e.target.value)}\n    /&gt;\n  );\n}\n\n// 非制御コンポーネント：DOMが状態を管理\nfunction UncontrolledInput() {\n  const inputRef = useRef(null);\n  \n  const handleSubmit = () =&gt; {\n    console.log(inputRef.current.value);  // DOMから直接取得\n  };\n  \n  return (\n    &lt;input ref={inputRef} defaultValue=\"初期値\" /&gt;\n  );\n}</code></pre></div></div><p class=\"content-text\">通常は<strong>制御コンポーネント</strong>を使うことが推奨されます。リアルタイムバリデーションや条件付き入力制御が容易になります。</p></div><div class=\"content-section\"><h2 class=\"section-title\">状態の初期化</h2><p class=\"content-text\">初期値の計算が重い場合は、<strong>関数を渡す</strong>ことで最適化できます：</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>// ❌ 毎回計算される（非効率）\nconst [data, setData] = useState(expensiveComputation());\n\n// ✅ 初回のみ計算される（効率的）\nconst [data, setData] = useState(() =&gt; expensiveComputation());\n\n// 例：localStorageからの読み込み\nconst [todos, setTodos] = useState(() =&gt; {\n  const saved = localStorage.getItem('todos');\n  return saved ? JSON.parse(saved) : [];\n});</code></pre></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">まとめ</h2><p class=\"content-text\">この章で学んだこと：</p><ul><li><strong>useState</strong> - 関数コンポーネントで状態を管理するHook</li><li><strong>関数型更新</strong> - prev => ... で前の状態を確実に参照</li><li><strong>オブジェクトの更新</strong> - スプレッド構文で新しいオブジェクトを作成</li><li><strong>配列の更新</strong> - map, filter, スプレッド構文で新しい配列を作成</li><li><strong>状態のリフトアップ</strong> - 共通の親に状態を移動して共有</li><li><strong>制御コンポーネント</strong> - Reactが入力値を管理するパターン</li></ul><p class=\"content-text\">次のレッスンでは、<strong>useEffect</strong>を使った副作用の処理について学びます。</p></div>",
  "exercises": [
    {
      "question": "useStateの戻り値として正しいものはどれですか？",
      "options": [
        "現在の状態値のみ",
        "状態を更新する関数のみ",
        "[現在の状態値, 状態を更新する関数] の配列",
        "{ value: 状態値, setValue: 更新関数 } のオブジェクト"
      ],
      "answer": 2,
      "explanation": "useStateは配列を返し、1番目の要素が現在の状態値、2番目の要素が状態を更新する関数です。分割代入で const [count, setCount] = useState(0) のように受け取ります。"
    },
    {
      "question": "関数型更新 setCount(prev => prev + 1) を使うべき場面はどれですか？",
      "options": [
        "初期値を設定するとき",
        "状態を完全に新しい値に置き換えるとき",
        "前の状態に基づいて新しい状態を計算するとき",
        "状態をリセットするとき"
      ],
      "answer": 2,
      "explanation": "関数型更新は、前の状態に基づいて新しい状態を計算する場合に使います。Reactは状態更新をバッチ処理するため、関数型更新を使うことで常に最新の状態を参照できます。"
    },
    {
      "question": "オブジェクトの状態を更新する正しい方法はどれですか？",
      "options": [
        "user.name = '新しい名前' で直接変更する",
        "setUser({ name: '新しい名前' }) で上書きする",
        "setUser({ ...user, name: '新しい名前' }) でスプレッド構文を使う",
        "setUser(user.name = '新しい名前') で代入しながら更新する"
      ],
      "answer": 2,
      "explanation": "オブジェクトの状態を更新する際は、スプレッド構文で既存のプロパティをコピーし、変更したいプロパティだけを上書きした新しいオブジェクトを作成します。直接変更しても再レンダリングされません。"
    },
    {
      "question": "「状態のリフトアップ」とは何ですか？",
      "options": [
        "状態を子コンポーネントに移動すること",
        "状態を共通の親コンポーネントに移動して複数の子で共有すること",
        "状態をグローバル変数に保存すること",
        "状態をlocalStorageに保存すること"
      ],
      "answer": 1,
      "explanation": "状態のリフトアップとは、複数のコンポーネントで同じ状態を共有したい場合に、その状態を共通の親コンポーネントに移動することです。親から子へはPropsで値と更新関数を渡します。"
    }
  ]
}
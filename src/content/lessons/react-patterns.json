{
  "id": "react-patterns",
  "category": "react",
  "title": "実践パターン - コンポーネント設計とベストプラクティス",
  "description": "Reactの実践的なパターン、コンポーネント設計、ファイル構成、パフォーマンス最適化について学びましょう",
  "content": "<div class=\"content-section\"><h2 class=\"section-title\">実践的なReact開発</h2><p class=\"content-text\">これまでReactの基礎を学んできました。この章では、実際のプロジェクトで役立つ<strong>コンポーネント設計のベストプラクティス</strong>、<strong>ファイル・フォルダ構成</strong>、<strong>パフォーマンス最適化</strong>、そして<strong>実践的なTodoアプリの実装例</strong>を学びます。</p><div class=\"info-box\"><span class=\"info-box-icon\">💡</span><div class=\"info-box-content\"><div class=\"info-box-title\">この章で学ぶこと</div><div class=\"info-box-text\">1. コンポーネント設計の原則<br/>2. プロジェクトのファイル構成<br/>3. パフォーマンス最適化の基本<br/>4. 実践的なTodoアプリの実装</div></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">コンポーネント設計の原則</h2><p class=\"content-text\">良いコンポーネント設計には以下の原則があります：</p><ul><li><strong>単一責任の原則</strong> - 1つのコンポーネントは1つの役割に集中</li><li><strong>再利用性</strong> - 汎用的に使えるコンポーネントを作る</li><li><strong>適切な粒度</strong> - 大きすぎず、小さすぎない適切なサイズ</li><li><strong>明確なインターフェース</strong> - Propsで何を受け取るか明確にする</li></ul><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>// ❌ 悪い例：1つのコンポーネントに複数の責任\\nfunction UserPage() {\\n  const [user, setUser] = useState(null);\\n  const [posts, setPosts] = useState([]);\\n  // ユーザー取得、投稿取得、ローディング管理...\\n  return (\\n    &lt;div&gt;{/* ヘッダー、プロフィール、投稿一覧... */}&lt;/div&gt;\\n  );\\n}\\n\\n// ✅ 良い例：責任を分割\\nfunction UserPage() {\\n  return (\\n    &lt;div&gt;\\n      &lt;UserProfile /&gt;\\n      &lt;UserPosts /&gt;\\n    &lt;/div&gt;\\n  );\\n}</code></pre></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">Presentational と Container パターン</h2><p class=\"content-text\">コンポーネントを「見た目」と「ロジック」で分離するパターンです：</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>// Presentational Component（見た目のみ）\\nfunction TodoItem({ todo, onToggle, onDelete }) {\\n  return (\\n    &lt;li className={todo.completed ? 'completed' : ''}&gt;\\n      &lt;input\\n        type=\\\"checkbox\\\"\\n        checked={todo.completed}\\n        onChange={() =&gt; onToggle(todo.id)}\\n      /&gt;\\n      &lt;span&gt;{todo.text}&lt;/span&gt;\\n      &lt;button onClick={() =&gt; onDelete(todo.id)}&gt;削除&lt;/button&gt;\\n    &lt;/li&gt;\\n  );\\n}\\n\\n// Container Component（ロジック担当）\\nfunction TodoListContainer() {\\n  const [todos, setTodos] = useState([]);\\n\\n  const handleToggle = (id) =&gt; {\\n    setTodos(todos.map(todo =&gt;\\n      todo.id === id ? { ...todo, completed: !todo.completed } : todo\\n    ));\\n  };\\n\\n  const handleDelete = (id) =&gt; {\\n    setTodos(todos.filter(todo =&gt; todo.id !== id));\\n  };\\n\\n  return (\\n    &lt;ul&gt;\\n      {todos.map(todo =&gt; (\\n        &lt;TodoItem\\n          key={todo.id}\\n          todo={todo}\\n          onToggle={handleToggle}\\n          onDelete={handleDelete}\\n        /&gt;\\n      ))}\\n    &lt;/ul&gt;\\n  );\\n}</code></pre></div></div><div class=\"info-box\"><span class=\"info-box-icon\">💡</span><div class=\"info-box-content\"><div class=\"info-box-title\">現代のアプローチ</div><div class=\"info-box-text\">Hooksの登場により、このパターンは以前ほど厳密に分ける必要はなくなりました。カスタムHooksでロジックを分離する方法が主流です。</div></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">プロジェクトのファイル構成</h2><p class=\"content-text\">Reactプロジェクトのファイル構成には様々なパターンがあります。代表的な2つを紹介します：</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">TEXT</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>// パターン1: 種類別（小〜中規模向け）\\nsrc/\\n├── components/     # 共通コンポーネント\\n│   ├── Button.jsx\\n│   ├── Modal.jsx\\n│   └── Card.jsx\\n├── pages/          # ページコンポーネント\\n│   ├── Home.jsx\\n│   └── About.jsx\\n├── hooks/          # カスタムHooks\\n│   ├── useFetch.js\\n│   └── useAuth.js\\n├── contexts/       # Context\\n│   └── AuthContext.jsx\\n├── utils/          # ユーティリティ関数\\n│   └── helpers.js\\n└── App.jsx</code></pre></div></div><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">TEXT</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>// パターン2: 機能別（中〜大規模向け）\\nsrc/\\n├── features/\\n│   ├── auth/\\n│   │   ├── components/\\n│   │   ├── hooks/\\n│   │   ├── AuthContext.jsx\\n│   │   └── index.js\\n│   ├── todos/\\n│   │   ├── components/\\n│   │   ├── hooks/\\n│   │   └── index.js\\n│   └── users/\\n├── shared/         # 共通部品\\n│   ├── components/\\n│   └── hooks/\\n└── App.jsx</code></pre></div></div><div class=\"info-box\"><span class=\"info-box-icon\">💡</span><div class=\"info-box-content\"><div class=\"info-box-title\">構成のポイント</div><div class=\"info-box-text\">・小規模なら種類別、大規模なら機能別が管理しやすい<br/>・index.jsでエクスポートをまとめると、インポートがシンプルになる<br/>・チームで統一したルールを決めることが重要</div></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">パフォーマンス最適化の基本</h2><p class=\"content-text\">Reactアプリのパフォーマンスを改善する主な方法：</p><ul><li><strong>React.memo</strong> - コンポーネントのメモ化</li><li><strong>useMemo / useCallback</strong> - 値・関数のメモ化</li><li><strong>コード分割</strong> - 必要な時に必要なコードだけ読み込む</li><li><strong>仮想化</strong> - 大量リストの効率的なレンダリング</li></ul><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>import { memo, useMemo, useCallback, lazy, Suspense } from 'react';\\n\\n// 1. React.memo - Propsが変わらなければ再レンダリングしない\\nconst ExpensiveComponent = memo(function ExpensiveComponent({ data }) {\\n  return &lt;div&gt;{/* 重い処理 */}&lt;/div&gt;;\\n});\\n\\n// 2. useMemo - 計算結果をキャッシュ\\nfunction FilteredList({ items, filter }) {\\n  const filtered = useMemo(() =&gt;\\n    items.filter(item =&gt; item.includes(filter)),\\n    [items, filter]\\n  );\\n  return &lt;ul&gt;{filtered.map(i =&gt; &lt;li key={i}&gt;{i}&lt;/li&gt;)}&lt;/ul&gt;;\\n}\\n\\n// 3. useCallback - 関数をメモ化\\nfunction Parent() {\\n  const handleClick = useCallback(() =&gt; {\\n    console.log('clicked');\\n  }, []);\\n  return &lt;ExpensiveComponent onClick={handleClick} /&gt;;\\n}\\n\\n// 4. lazy + Suspense - コード分割\\nconst HeavyComponent = lazy(() =&gt; import('./HeavyComponent'));\\n\\nfunction App() {\\n  return (\\n    &lt;Suspense fallback={&lt;div&gt;Loading...&lt;/div&gt;}&gt;\\n      &lt;HeavyComponent /&gt;\\n    &lt;/Suspense&gt;\\n  );\\n}</code></pre></div></div><div class=\"info-box\"><span class=\"info-box-icon\">⚠️</span><div class=\"info-box-content\"><div class=\"info-box-title\">最適化の注意点</div><div class=\"info-box-text\">「早すぎる最適化は諸悪の根源」- まずは普通に書いて、問題が発生してから最適化しましょう。React DevToolsのProfilerで実際のパフォーマンスを計測することが重要です。</div></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">実践：Todoアプリの実装</h2><p class=\"content-text\">これまで学んだ知識を使って、実践的なTodoアプリを実装してみましょう。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>// App.jsx - メインコンポーネント\\nimport { useState } from 'react';\\n\\nfunction App() {\\n  const [todos, setTodos] = useState([]);\\n  const [filter, setFilter] = useState('all');\\n\\n  const addTodo = (text) =&gt; {\\n    setTodos([...todos, {\\n      id: Date.now(),\\n      text,\\n      completed: false\\n    }]);\\n  };\\n\\n  const toggleTodo = (id) =&gt; {\\n    setTodos(todos.map(todo =&gt;\\n      todo.id === id\\n        ? { ...todo, completed: !todo.completed }\\n        : todo\\n    ));\\n  };\\n\\n  const deleteTodo = (id) =&gt; {\\n    setTodos(todos.filter(todo =&gt; todo.id !== id));\\n  };\\n\\n  const filteredTodos = todos.filter(todo =&gt; {\\n    if (filter === 'active') return !todo.completed;\\n    if (filter === 'completed') return todo.completed;\\n    return true;\\n  });\\n\\n  return (\\n    &lt;div className=\\\"app\\\"&gt;\\n      &lt;h1&gt;Todo App&lt;/h1&gt;\\n      &lt;TodoForm onAdd={addTodo} /&gt;\\n      &lt;FilterButtons filter={filter} onFilterChange={setFilter} /&gt;\\n      &lt;TodoList\\n        todos={filteredTodos}\\n        onToggle={toggleTodo}\\n        onDelete={deleteTodo}\\n      /&gt;\\n      &lt;TodoStats todos={todos} /&gt;\\n    &lt;/div&gt;\\n  );\\n}</code></pre></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">Todoアプリ：子コンポーネント</h2><p class=\"content-text\">各機能を担当するコンポーネントを作成します：</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>// TodoForm.jsx - 入力フォーム\\nfunction TodoForm({ onAdd }) {\\n  const [text, setText] = useState('');\\n\\n  const handleSubmit = (e) =&gt; {\\n    e.preventDefault();\\n    if (text.trim()) {\\n      onAdd(text.trim());\\n      setText('');\\n    }\\n  };\\n\\n  return (\\n    &lt;form onSubmit={handleSubmit}&gt;\\n      &lt;input\\n        value={text}\\n        onChange={(e) =&gt; setText(e.target.value)}\\n        placeholder=\\\"新しいタスクを入力\\\"\\n      /&gt;\\n      &lt;button type=\\\"submit\\\"&gt;追加&lt;/button&gt;\\n    &lt;/form&gt;\\n  );\\n}\\n\\n// TodoList.jsx - リスト表示\\nfunction TodoList({ todos, onToggle, onDelete }) {\\n  if (todos.length === 0) {\\n    return &lt;p&gt;タスクがありません&lt;/p&gt;;\\n  }\\n\\n  return (\\n    &lt;ul className=\\\"todo-list\\\"&gt;\\n      {todos.map(todo =&gt; (\\n        &lt;TodoItem\\n          key={todo.id}\\n          todo={todo}\\n          onToggle={onToggle}\\n          onDelete={onDelete}\\n        /&gt;\\n      ))}\\n    &lt;/ul&gt;\\n  );\\n}\\n\\n// TodoItem.jsx - 個別アイテム\\nfunction TodoItem({ todo, onToggle, onDelete }) {\\n  return (\\n    &lt;li className={todo.completed ? 'completed' : ''}&gt;\\n      &lt;input\\n        type=\\\"checkbox\\\"\\n        checked={todo.completed}\\n        onChange={() =&gt; onToggle(todo.id)}\\n      /&gt;\\n      &lt;span&gt;{todo.text}&lt;/span&gt;\\n      &lt;button onClick={() =&gt; onDelete(todo.id)}&gt;×&lt;/button&gt;\\n    &lt;/li&gt;\\n  );\\n}</code></pre></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">Todoアプリ：フィルターと統計</h2><p class=\"content-text\">フィルター機能と統計表示を追加します：</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>// FilterButtons.jsx - フィルターボタン\\nfunction FilterButtons({ filter, onFilterChange }) {\\n  const filters = [\\n    { value: 'all', label: 'すべて' },\\n    { value: 'active', label: '未完了' },\\n    { value: 'completed', label: '完了済み' }\\n  ];\\n\\n  return (\\n    &lt;div className=\\\"filters\\\"&gt;\\n      {filters.map(f =&gt; (\\n        &lt;button\\n          key={f.value}\\n          className={filter === f.value ? 'active' : ''}\\n          onClick={() =&gt; onFilterChange(f.value)}\\n        &gt;\\n          {f.label}\\n        &lt;/button&gt;\\n      ))}\\n    &lt;/div&gt;\\n  );\\n}\\n\\n// TodoStats.jsx - 統計表示\\nfunction TodoStats({ todos }) {\\n  const total = todos.length;\\n  const completed = todos.filter(t =&gt; t.completed).length;\\n  const active = total - completed;\\n\\n  return (\\n    &lt;div className=\\\"stats\\\"&gt;\\n      &lt;span&gt;全{total}件&lt;/span&gt;\\n      &lt;span&gt;未完了: {active}件&lt;/span&gt;\\n      &lt;span&gt;完了: {completed}件&lt;/span&gt;\\n    &lt;/div&gt;\\n  );\\n}</code></pre></div></div><div class=\"info-box\"><span class=\"info-box-icon\">💡</span><div class=\"info-box-content\"><div class=\"info-box-title\">発展課題</div><div class=\"info-box-text\">・localStorageで永続化（useLocalStorageカスタムHook）<br/>・ドラッグ&amp;ドロップで並び替え<br/>・期限日の設定と通知<br/>・カテゴリ分け機能</div></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">エラーバウンダリ</h2><p class=\"content-text\">コンポーネントでエラーが発生した時に、アプリ全体がクラッシュするのを防ぐ仕組みです：</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>// ErrorBoundary.jsx（クラスコンポーネントで実装）\\nimport { Component } from 'react';\\n\\nclass ErrorBoundary extends Component {\\n  state = { hasError: false, error: null };\\n\\n  static getDerivedStateFromError(error) {\\n    return { hasError: true, error };\\n  }\\n\\n  componentDidCatch(error, errorInfo) {\\n    console.error('Error caught:', error, errorInfo);\\n  }\\n\\n  render() {\\n    if (this.state.hasError) {\\n      return (\\n        &lt;div className=\\\"error-fallback\\\"&gt;\\n          &lt;h2&gt;エラーが発生しました&lt;/h2&gt;\\n          &lt;button onClick={() =&gt; this.setState({ hasError: false })}&gt;\\n            再試行\\n          &lt;/button&gt;\\n        &lt;/div&gt;\\n      );\\n    }\\n    return this.props.children;\\n  }\\n}\\n\\n// 使用例\\nfunction App() {\\n  return (\\n    &lt;ErrorBoundary&gt;\\n      &lt;MainContent /&gt;\\n    &lt;/ErrorBoundary&gt;\\n  );\\n}</code></pre></div></div><div class=\"info-box\"><span class=\"info-box-icon\">💡</span><div class=\"info-box-content\"><div class=\"info-box-title\">エラーバウンダリの注意点</div><div class=\"info-box-text\">・イベントハンドラ内のエラーはキャッチできない<br/>・非同期コード（setTimeout等）のエラーもキャッチできない<br/>・サーバーサイドレンダリングでは動作しない</div></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">まとめ</h2><p class=\"content-text\">この章で学んだこと：</p><ul><li><strong>コンポーネント設計</strong> - 単一責任、再利用性、適切な粒度</li><li><strong>ファイル構成</strong> - 種類別 vs 機能別の構成パターン</li><li><strong>パフォーマンス最適化</strong> - memo、useMemo、useCallback、コード分割</li><li><strong>Todoアプリ実装</strong> - 実践的なコンポーネント分割と状態管理</li><li><strong>エラーバウンダリ</strong> - エラーハンドリングの仕組み</li></ul><p class=\"content-text\">これでReactの基礎から実践パターンまで学習が完了しました。次のステップとして、React Router（ルーティング）、状態管理ライブラリ（Redux、Zustand）、Next.js（フルスタックフレームワーク）などを学んでいくと良いでしょう。</p></div>",
  "exercises": [
    {
      "question": "コンポーネント設計の「単一責任の原則」として正しいものはどれですか？",
      "options": [
        "1つのコンポーネントで全ての機能を実装する",
        "1つのコンポーネントは1つの役割に集中する",
        "コンポーネントは必ず100行以内にする",
        "全てのコンポーネントでuseStateを使う"
      ],
      "answer": 1,
      "explanation": "単一責任の原則とは、1つのコンポーネントが1つの役割・責任に集中することです。これにより、コードの理解しやすさ、テストのしやすさ、再利用性が向上します。"
    },
    {
      "question": "React.memoの役割として正しいものはどれですか？",
      "options": [
        "計算結果をキャッシュする",
        "関数をメモ化する",
        "Propsが変わらなければコンポーネントの再レンダリングをスキップする",
        "状態を永続化する"
      ],
      "answer": 2,
      "explanation": "React.memoは高階コンポーネントで、Propsが変わらない場合にコンポーネントの再レンダリングをスキップします。計算結果のキャッシュはuseMemo、関数のメモ化はuseCallbackの役割です。"
    },
    {
      "question": "Reactプロジェクトのファイル構成で「機能別」構成が適しているのはどのような場合ですか？",
      "options": [
        "小規模なプロジェクト",
        "コンポーネントが10個以下のプロジェクト",
        "中〜大規模なプロジェクト",
        "個人開発のプロジェクト"
      ],
      "answer": 2,
      "explanation": "機能別構成（features/auth/, features/todos/など）は、中〜大規模なプロジェクトに適しています。関連するコンポーネント、hooks、contextが同じフォルダにまとまるため、機能単位での管理がしやすくなります。"
    },
    {
      "question": "エラーバウンダリがキャッチできないエラーはどれですか？",
      "options": [
        "子コンポーネントのレンダリング中のエラー",
        "ライフサイクルメソッド内のエラー",
        "イベントハンドラ内のエラー",
        "コンストラクタ内のエラー"
      ],
      "answer": 2,
      "explanation": "エラーバウンダリは、イベントハンドラ内のエラー、非同期コード（setTimeout、fetch等）のエラー、サーバーサイドレンダリングのエラーはキャッチできません。イベントハンドラ内のエラーは通常のtry-catchで処理する必要があります。"
    }
  ]
}
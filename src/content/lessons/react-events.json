{
  "id": "react-events",
  "category": "react",
  "title": "イベント処理とフォーム",
  "description": "Reactでのイベントハンドラの基本、SyntheticEvent、制御コンポーネントと非制御コンポーネント、フォームの実装パターンについて学びましょう",
  "content": "<div class=\"content-section\"><h2 class=\"section-title\">Reactのイベント処理</h2><p class=\"content-text\">Reactでは、DOMイベントを<strong>キャメルケース</strong>で記述し、JSXでは文字列ではなく<strong>関数</strong>をイベントハンドラとして渡します。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>// HTML\n&lt;button onclick=\"handleClick()\"&gt;クリック&lt;/button&gt;\n\n// React\n&lt;button onClick={handleClick}&gt;クリック&lt;/button&gt;</code></pre></div></div><p class=\"content-text\">主な違い：</p><ul><li><strong>onclick</strong> → <strong>onClick</strong>（キャメルケース）</li><li><strong>\"handleClick()\"</strong> → <strong>{handleClick}</strong>（関数を渡す）</li><li>イベント名はすべてキャメルケース（onSubmit, onChange, onMouseEnter等）</li></ul></div><div class=\"content-section\"><h2 class=\"section-title\">イベントハンドラの定義</h2><p class=\"content-text\">イベントハンドラは、コンポーネント内で関数として定義します。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>function Button() {\n  // イベントハンドラを定義\n  const handleClick = () =&gt; {\n    alert('ボタンがクリックされました！');\n  };\n\n  return (\n    &lt;button onClick={handleClick}&gt;\n      クリック\n    &lt;/button&gt;\n  );\n}\n\n// インラインで定義することも可能\nfunction InlineButton() {\n  return (\n    &lt;button onClick={() =&gt; alert('クリック！')}&gt;\n      クリック\n    &lt;/button&gt;\n  );\n}</code></pre></div></div><div class=\"info-box\"><span class=\"info-box-icon\">⚠️</span><div class=\"info-box-content\"><div class=\"info-box-title\">よくある間違い</div><div class=\"info-box-text\">❌ onClick={handleClick()} - 関数を呼び出してしまう<br/>✅ onClick={handleClick} - 関数を渡す<br/>括弧をつけると、レンダリング時に即座に実行されてしまいます。</div></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">イベントハンドラに引数を渡す</h2><p class=\"content-text\">イベントハンドラに追加の引数を渡したい場合は、アロー関数でラップします。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>function ItemList() {\n  const items = ['りんご', 'バナナ', 'オレンジ'];\n\n  const handleDelete = (itemName) =&gt; {\n    alert(`${itemName}を削除しました`);\n  };\n\n  return (\n    &lt;ul&gt;\n      {items.map(item =&gt; (\n        &lt;li key={item}&gt;\n          {item}\n          &lt;button onClick={() =&gt; handleDelete(item)}&gt;\n            削除\n          &lt;/button&gt;\n        &lt;/li&gt;\n      ))}\n    &lt;/ul&gt;\n  );\n}</code></pre></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">SyntheticEvent</h2><p class=\"content-text\">Reactのイベントハンドラは、ブラウザのネイティブイベントではなく<strong>SyntheticEvent</strong>（合成イベント）を受け取ります。これはブラウザ間の差異を吸収し、一貫したAPIを提供します。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>function EventExample() {\n  const handleClick = (event) =&gt; {\n    // SyntheticEventのプロパティ\n    console.log(event.type);        // 'click'\n    console.log(event.target);      // クリックされた要素\n    console.log(event.currentTarget); // ハンドラがアタッチされた要素\n    \n    // ネイティブイベントにアクセス\n    console.log(event.nativeEvent);\n  };\n\n  return &lt;button onClick={handleClick}&gt;クリック&lt;/button&gt;;\n}</code></pre></div></div><p class=\"content-text\">よく使うSyntheticEventのプロパティ：</p><ul><li><strong>event.target</strong> - イベントが発生した要素</li><li><strong>event.currentTarget</strong> - イベントハンドラがアタッチされた要素</li><li><strong>event.preventDefault()</strong> - デフォルト動作を防ぐ</li><li><strong>event.stopPropagation()</strong> - イベントの伝播を止める</li></ul></div><div class=\"content-section\"><h2 class=\"section-title\">イベントの伝播とpreventDefault</h2><p class=\"content-text\">イベントは子要素から親要素へと<strong>バブリング</strong>（伝播）します。また、フォーム送信などのデフォルト動作を防ぐことも重要です。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>function Form() {\n  const handleSubmit = (event) =&gt; {\n    // フォームのデフォルト送信を防ぐ\n    event.preventDefault();\n    console.log('フォームが送信されました');\n  };\n\n  return (\n    &lt;form onSubmit={handleSubmit}&gt;\n      &lt;input type=\"text\" /&gt;\n      &lt;button type=\"submit\"&gt;送信&lt;/button&gt;\n    &lt;/form&gt;\n  );\n}\n\nfunction NestedButtons() {\n  const handleParentClick = () =&gt; {\n    console.log('親がクリックされた');\n  };\n\n  const handleChildClick = (event) =&gt; {\n    // 親への伝播を止める\n    event.stopPropagation();\n    console.log('子がクリックされた');\n  };\n\n  return (\n    &lt;div onClick={handleParentClick}&gt;\n      &lt;button onClick={handleChildClick}&gt;\n        子ボタン\n      &lt;/button&gt;\n    &lt;/div&gt;\n  );\n}</code></pre></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">制御コンポーネント</h2><p class=\"content-text\"><strong>制御コンポーネント</strong>は、フォーム要素の値をReactの状態で管理するパターンです。入力値は常にReactの状態と同期されます。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>import { useState } from 'react';\n\nfunction ControlledInput() {\n  const [value, setValue] = useState('');\n\n  const handleChange = (event) =&gt; {\n    setValue(event.target.value);\n  };\n\n  return (\n    &lt;div&gt;\n      &lt;input\n        type=\"text\"\n        value={value}  // Reactの状態を値として設定\n        onChange={handleChange}  // 変更時に状態を更新\n      /&gt;\n      &lt;p&gt;入力値: {value}&lt;/p&gt;\n    &lt;/div&gt;\n  );\n}</code></pre></div></div><div class=\"info-box\"><span class=\"info-box-icon\">💡</span><div class=\"info-box-content\"><div class=\"info-box-title\">制御コンポーネントの利点</div><div class=\"info-box-text\">・入力値をリアルタイムで検証できる<br/>・入力を条件付きで制限できる<br/>・複数の入力を連動させやすい<br/>・フォームの状態を完全に把握できる</div></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">非制御コンポーネント</h2><p class=\"content-text\"><strong>非制御コンポーネント</strong>は、DOMが入力値を管理するパターンです。useRefを使ってDOM要素に直接アクセスします。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>import { useRef } from 'react';\n\nfunction UncontrolledInput() {\n  const inputRef = useRef(null);\n\n  const handleSubmit = (event) =&gt; {\n    event.preventDefault();\n    // DOMから直接値を取得\n    alert(`入力値: ${inputRef.current.value}`);\n  };\n\n  return (\n    &lt;form onSubmit={handleSubmit}&gt;\n      &lt;input\n        type=\"text\"\n        ref={inputRef}\n        defaultValue=\"初期値\"  // valueではなくdefaultValue\n      /&gt;\n      &lt;button type=\"submit\"&gt;送信&lt;/button&gt;\n    &lt;/form&gt;\n  );\n}</code></pre></div></div><p class=\"content-text\">制御 vs 非制御の使い分け：</p><ul><li><strong>制御コンポーネント</strong>：リアルタイム検証、条件付き入力制限が必要な場合</li><li><strong>非制御コンポーネント</strong>：シンプルなフォーム、ファイル入力、サードパーティライブラリとの統合</li></ul></div><div class=\"content-section\"><h2 class=\"section-title\">様々なフォーム要素</h2><p class=\"content-text\">Reactでは様々なフォーム要素を制御コンポーネントとして扱えます。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>function FormElements() {\n  const [text, setText] = useState('');\n  const [textarea, setTextarea] = useState('');\n  const [selected, setSelected] = useState('apple');\n  const [checked, setChecked] = useState(false);\n\n  return (\n    &lt;form&gt;\n      {/* テキスト入力 */}\n      &lt;input\n        type=\"text\"\n        value={text}\n        onChange={(e) =&gt; setText(e.target.value)}\n      /&gt;\n\n      {/* テキストエリア */}\n      &lt;textarea\n        value={textarea}\n        onChange={(e) =&gt; setTextarea(e.target.value)}\n      /&gt;\n\n      {/* セレクトボックス */}\n      &lt;select\n        value={selected}\n        onChange={(e) =&gt; setSelected(e.target.value)}\n      &gt;\n        &lt;option value=\"apple\"&gt;りんご&lt;/option&gt;\n        &lt;option value=\"banana\"&gt;バナナ&lt;/option&gt;\n        &lt;option value=\"orange\"&gt;オレンジ&lt;/option&gt;\n      &lt;/select&gt;\n\n      {/* チェックボックス */}\n      &lt;input\n        type=\"checkbox\"\n        checked={checked}\n        onChange={(e) =&gt; setChecked(e.target.checked)}\n      /&gt;\n    &lt;/form&gt;\n  );\n}</code></pre></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">複数の入力を1つのハンドラで処理</h2><p class=\"content-text\">name属性を使って、複数の入力フィールドを1つのハンドラで処理できます。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>function MultipleInputs() {\n  const [formData, setFormData] = useState({\n    username: '',\n    email: '',\n    password: ''\n  });\n\n  const handleChange = (event) =&gt; {\n    const { name, value } = event.target;\n    setFormData(prev =&gt; ({\n      ...prev,\n      [name]: value  // 計算されたプロパティ名\n    }));\n  };\n\n  return (\n    &lt;form&gt;\n      &lt;input\n        name=\"username\"\n        value={formData.username}\n        onChange={handleChange}\n        placeholder=\"ユーザー名\"\n      /&gt;\n      &lt;input\n        name=\"email\"\n        type=\"email\"\n        value={formData.email}\n        onChange={handleChange}\n        placeholder=\"メールアドレス\"\n      /&gt;\n      &lt;input\n        name=\"password\"\n        type=\"password\"\n        value={formData.password}\n        onChange={handleChange}\n        placeholder=\"パスワード\"\n      /&gt;\n    &lt;/form&gt;\n  );\n}</code></pre></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">フォームバリデーション</h2><p class=\"content-text\">制御コンポーネントを使うと、リアルタイムでバリデーションを実装できます。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>function ValidatedForm() {\n  const [email, setEmail] = useState('');\n  const [password, setPassword] = useState('');\n  const [errors, setErrors] = useState({});\n\n  const validateEmail = (value) =&gt; {\n    if (!value) return 'メールアドレスは必須です';\n    if (!value.includes('@')) return '有効なメールアドレスを入力してください';\n    return '';\n  };\n\n  const validatePassword = (value) =&gt; {\n    if (!value) return 'パスワードは必須です';\n    if (value.length &lt; 8) return 'パスワードは8文字以上必要です';\n    return '';\n  };\n\n  const handleEmailChange = (e) =&gt; {\n    const value = e.target.value;\n    setEmail(value);\n    setErrors(prev =&gt; ({ ...prev, email: validateEmail(value) }));\n  };\n\n  const handlePasswordChange = (e) =&gt; {\n    const value = e.target.value;\n    setPassword(value);\n    setErrors(prev =&gt; ({ ...prev, password: validatePassword(value) }));\n  };\n\n  const handleSubmit = (e) =&gt; {\n    e.preventDefault();\n    const emailError = validateEmail(email);\n    const passwordError = validatePassword(password);\n    \n    if (emailError || passwordError) {\n      setErrors({ email: emailError, password: passwordError });\n      return;\n    }\n    \n    console.log('送信:', { email, password });\n  };\n\n  return (\n    &lt;form onSubmit={handleSubmit}&gt;\n      &lt;div&gt;\n        &lt;input\n          type=\"email\"\n          value={email}\n          onChange={handleEmailChange}\n          placeholder=\"メールアドレス\"\n        /&gt;\n        {errors.email &amp;&amp; &lt;span className=\"error\"&gt;{errors.email}&lt;/span&gt;}\n      &lt;/div&gt;\n      &lt;div&gt;\n        &lt;input\n          type=\"password\"\n          value={password}\n          onChange={handlePasswordChange}\n          placeholder=\"パスワード\"\n        /&gt;\n        {errors.password &amp;&amp; &lt;span className=\"error\"&gt;{errors.password}&lt;/span&gt;}\n      &lt;/div&gt;\n      &lt;button type=\"submit\"&gt;送信&lt;/button&gt;\n    &lt;/form&gt;\n  );\n}</code></pre></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">実践的なフォーム実装例</h2><p class=\"content-text\">ローディング状態やエラーハンドリングを含む、実践的なフォームの例です。</p><div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">JSX</span><button class=\"code-copy\" onclick=\"copyCode(this)\">コピー</button></div><div class=\"code-content\"><pre><code>function ContactForm() {\n  const [formData, setFormData] = useState({\n    name: '',\n    email: '',\n    message: ''\n  });\n  const [isSubmitting, setIsSubmitting] = useState(false);\n  const [submitStatus, setSubmitStatus] = useState(null);\n\n  const handleChange = (e) =&gt; {\n    const { name, value } = e.target;\n    setFormData(prev =&gt; ({ ...prev, [name]: value }));\n  };\n\n  const handleSubmit = async (e) =&gt; {\n    e.preventDefault();\n    setIsSubmitting(true);\n    setSubmitStatus(null);\n\n    try {\n      // APIへの送信をシミュレート\n      await new Promise(resolve =&gt; setTimeout(resolve, 1000));\n      setSubmitStatus('success');\n      setFormData({ name: '', email: '', message: '' });\n    } catch (error) {\n      setSubmitStatus('error');\n    } finally {\n      setIsSubmitting(false);\n    }\n  };\n\n  return (\n    &lt;form onSubmit={handleSubmit}&gt;\n      &lt;input\n        name=\"name\"\n        value={formData.name}\n        onChange={handleChange}\n        placeholder=\"お名前\"\n        required\n      /&gt;\n      &lt;input\n        name=\"email\"\n        type=\"email\"\n        value={formData.email}\n        onChange={handleChange}\n        placeholder=\"メールアドレス\"\n        required\n      /&gt;\n      &lt;textarea\n        name=\"message\"\n        value={formData.message}\n        onChange={handleChange}\n        placeholder=\"メッセージ\"\n        required\n      /&gt;\n      &lt;button type=\"submit\" disabled={isSubmitting}&gt;\n        {isSubmitting ? '送信中...' : '送信'}\n      &lt;/button&gt;\n      {submitStatus === 'success' &amp;&amp; &lt;p&gt;送信完了！&lt;/p&gt;}\n      {submitStatus === 'error' &amp;&amp; &lt;p&gt;エラーが発生しました&lt;/p&gt;}\n    &lt;/form&gt;\n  );\n}</code></pre></div></div></div><div class=\"content-section\"><h2 class=\"section-title\">まとめ</h2><p class=\"content-text\">この章で学んだこと：</p><ul><li><strong>イベントハンドラ</strong> - キャメルケースで記述し、関数を渡す</li><li><strong>SyntheticEvent</strong> - ブラウザ間の差異を吸収した合成イベント</li><li><strong>preventDefault/stopPropagation</strong> - デフォルト動作の防止とイベント伝播の制御</li><li><strong>制御コンポーネント</strong> - Reactの状態でフォーム値を管理</li><li><strong>非制御コンポーネント</strong> - DOMが値を管理、useRefでアクセス</li><li><strong>フォームバリデーション</strong> - リアルタイムでの入力検証</li></ul><p class=\"content-text\">次のレッスンでは、<strong>useContext、useRef、useMemo、useCallback</strong>などの応用Hooksについて学びます。</p></div>",
  "exercises": [
    {
      "question": "Reactでイベントハンドラを正しく設定する方法はどれですか？",
      "options": [
        "onclick=\"handleClick()\"",
        "onClick={handleClick()}",
        "onClick={handleClick}",
        "on-click={handleClick}"
      ],
      "answer": 2,
      "explanation": "Reactではイベント名をキャメルケース（onClick）で記述し、関数自体を渡します。handleClick()と括弧をつけると、レンダリング時に即座に実行されてしまいます。"
    },
    {
      "question": "SyntheticEventについて正しい説明はどれですか？",
      "options": [
        "ブラウザのネイティブイベントそのもの",
        "Reactが提供する、ブラウザ間の差異を吸収した合成イベント",
        "非同期イベントを処理するための特別なイベント",
        "カスタムイベントを作成するためのAPI"
      ],
      "answer": 1,
      "explanation": "SyntheticEventは、Reactがブラウザのネイティブイベントをラップして提供する合成イベントです。これにより、異なるブラウザ間で一貫したAPIを使用できます。"
    },
    {
      "question": "制御コンポーネントの特徴として正しいものはどれですか？",
      "options": [
        "DOMが入力値を管理し、useRefでアクセスする",
        "Reactの状態で入力値を管理し、valueとonChangeを設定する",
        "defaultValueを使って初期値を設定する",
        "フォーム送信時にのみ値を取得できる"
      ],
      "answer": 1,
      "explanation": "制御コンポーネントでは、Reactの状態（useState）で入力値を管理します。value属性に状態を設定し、onChangeで状態を更新することで、入力値とReactの状態が常に同期されます。"
    },
    {
      "question": "フォーム送信時にページのリロードを防ぐ正しい方法はどれですか？",
      "options": [
        "event.stopPropagation()",
        "event.preventDefault()",
        "return false",
        "event.cancelBubble = true"
      ],
      "answer": 1,
      "explanation": "event.preventDefault()を呼び出すことで、フォーム送信のデフォルト動作（ページのリロード）を防ぐことができます。stopPropagation()はイベントの伝播を止めるもので、デフォルト動作の防止には使えません。"
    }
  ]
}